services:
  - type: web
    name: ai-resume-analyser
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server-entry.js
    healthCheckPath: /api
    envVars:
      - key: NODE_ENV
        value: production